# EL Bot v1.0.0 — Execution Status

Program status: ACTIVE
Master plan: `docs/V1.0.0_MASTER_ARCHITECTURE_PLAN.md`
Historical baseline: `docs/V0.6.0_HISTORICAL_BASELINE_FREEZE.md`
Repository rule: **main only**

## Execution sequence

### Step 1 — Freeze EL Bot v0.6.0 historical baseline

Status: **COMPLETE ✅**

Canonical historical release:

- version: `0.6.0`
- release commit: `454018106981ebfbcdf6468acb764b44dbccc7ca`
- release tag: `el-bot-v0.6.0-454018106981`
- release verification: `phase6-release-verification = success`

Freeze record includes package/install contract, legacy embedded Forgey snapshot, human-test successes, human-test defects, incomplete tests, migration boundaries, and anti-history-rewrite rules.

### Step 2 — Establish Forgey-Insta:EL-Bot independent home

Status: **COMPLETE ✅**

Canonical repository:

`doonchy16-cloud/forgey-insta-el-bot`

Establishing commit:

`728ca7c702f7b1da4c5ff2e2b498cd8ff3ed010e`

Independent remote-verification evidence commit:

`b98edb44c1d1bfb36a1fe561593b516c22960dac`

Step 2 established the independent Forgey product home, runtime/release contracts, empty generation registry, Owner/developer-only training boundary, evaluation/dataset/signing workspaces, legacy quarantine and anti-fake-G1 controls.

Most importantly, Step 2 did **not** declare, train, rename, or publish canonical G1.

### Step 3 — Inspect/migrate only justified legacy AI components

Status: **COMPLETE ✅**

Forgey commit:

`2cf7469971cd91cc84d4c80296c1f8dab1e7a601`

Frozen source audited:

`EL_BOT@454018106981ebfbcdf6468acb764b44dbccc7ca`

Step-3 forensic verdict:

- 21 components reviewed;
- 5 `MIGRATE`;
- 9 `REWRITE`;
- 5 `REFERENCE_ONLY`;
- 2 `REJECT`;
- 0 pending.

Only architecture-neutral foundations were physically migrated immediately: the generic immutable generation registry and configurable Ollama teacher client. Architecture-dependent model/tokenizer/training/vision work remained deferred to the canonical G1 design. EL Bot ranking/runtime orchestration was explicitly rejected from Forgey ownership.

The Forgey product remained PRE-G1 with an empty production registry throughout the migration.

### Step 4 — Freeze canonical Forgey G1 architecture/training contract

Status: **COMPLETE / LOCKED ✅🔒**

Forgey commit:

`f7fc9122d3359773b6d78c83566387784af1ef8c`

Architecture ID:

`forgey-insta-edt-v1-20m`

Frozen G1 design:

- multimodal PyTorch encoder-decoder Transformer;
- `d_model = 320`;
- 8 attention heads;
- 6 encoder + 6 decoder layers;
- FFN width 1280;
- 512-token context;
- deterministic Byte-BPE + Atomic EL tokenizer, exactly 4096 token IDs after freeze;
- native 128×128 RGB vision with 16×16 patches / 64 visual tokens;
- expected approximately 19–20M trainable parameters with a hard 18M–23M envelope;
- CPU FP32 inference mandatory; GPU acceleration optional;
- runtime ABI `forgey.runtime.v1`.

Step 4 also froze:

- training truth tiers and Owner/developer-only training authority;
- separated/fingerprinted TRAIN, PROTECTED_REPLAY, FROZEN_BENCHMARK, ADVERSARIAL, MODEL_ONLY_CANARY and vision splits;
- configurable teacher/provider policy with no hard-coded Qwen model and no provider-as-Forgey identity;
- raw-model-first evaluation so EL Bot repair/fallback cannot masquerade as Forgey success;
- anti-memorization/generalization benchmark contract;
- complete source-meaning accounting requirement;
- 30 hard G1 admission gates.

Canonical Step-4 authorities in Forgey:

- `docs/G1_ARCHITECTURE_AND_TRAINING_CONTRACT.md`
- `configs/g1-architecture.json`
- `configs/g1-tokenizer.json`
- `configs/g1-training.json`
- `configs/g1-teacher-policy.json`
- `datasets/contracts/g1-dataset-schema.json`
- `evaluation/contracts/g1-benchmark-contract.json`
- `evaluation/contracts/g1-admission-gates.json`

The old Step-2-only Actions workflow was replaced with one authoritative PRE-G1 contract-integrity workflow. Direct GitHub read-back verified the frozen product/architecture state, the empty generation registry, and `main`-only branch policy. The connector had not surfaced the new workflow's custom commit-status context during the verification window, so no CI PASS is fabricated.

Most importantly, Step 4 still reports:

- canonical generation: **NONE**;
- production generation: **NONE**;
- canonical G1 checkpoint: **DOES NOT EXIST YET**;
- canonical training before Step 5: **FORBIDDEN**.

### Step 5 — Implement/build/train canonical Forgey G1

Status: **NEXT 🔜**

Step 5 is the first step authorized to implement the frozen FI-EDT-20M architecture, build/freeze the canonical tokenizer and datasets, create an immutable run manifest, initialize a real G1 candidate, train it through the locked staged pipeline, and emit training evidence. Step 5 does **not** automatically admit or publish G1; later proof/admission/release gates remain separate.

### Step 6 — Prove actual learned-weight inference

Status: PENDING

### Step 7 — Certify/sign/publish Forgey G1

Status: PENDING

### Step 8 — Rewrite EL Bot as dynamic Forgey-release consumer

Status: PENDING

### Step 9 — Build Settings + trusted Forgey release selector

Status: PENDING

### Step 10 — Fix all frozen v0.6.0 defects

Status: PENDING

### Step 11 — Build EL Bot updater + Forgey model updater

Status: PENDING

### Step 12 — Remove user training + implement Doonchy-only Owner authorization boundary

Status: PENDING

### Step 13 — Full integrated brutal validation

Status: PENDING

### Step 14 — Publish EL Bot v1.0.0

Status: PENDING

## Change-control rule

Do not mark a step complete from intent, a green-looking UI, or a partial CI run. Each step requires explicit evidence satisfying its own locked completion gate.
