# EL Bot v0.7.0 Upgrade Plan

Status: LOCKED PLANNING SCOPE
Target: v0.7.0
Current released baseline under test: v0.6.0
Repository rule: main only; no feature/temp/staging branches.

## Purpose

v0.7.0 is the first post-v0.6.0 upgrade batch. It must combine the updater with all human-test defects discovered during v0.6.0 validation. The released v0.6.0 behavior stays frozen while evidence collection continues; fixes are implemented together as the v0.7.0 batch.

## Mandatory v0.7.0 scope

### 1. Full EL Bot updater

Build a user-facing updater that can:

- check the canonical EL Bot GitHub release source for a newer verified version;
- expose both automatic and manual `Check for Updates` behavior;
- compare semantic versions and never accidentally downgrade;
- show the available version and release notes/change summary;
- download the correct Windows installer/update payload with visible progress;
- retry/resume recoverably after network interruption;
- verify the downloaded payload against the release manifest and SHA-256 evidence before execution;
- preserve user settings/data across updates;
- safely hand off to the installer, restart EL Bot, and verify the new version after restart;
- report offline/unavailable/update-failed states truthfully;
- support the installed Setup flow and define safe behavior for Portable users;
- never report an update as installed until post-update verification succeeds.

### 2. ABC -> Emoji duplicate lexical/composition defect

Human-test reproduction:

- input: `credit card`
- expected canonical output: `💳`
- v0.6.0 observed winner: `💳 ➡️ 💳`
- alternatives showed the duplicated relation scoring about 321 with 100%/100%, while canonical `💳` scored about 193 with 50%/50%.

Required fix:

- investigate duplicate semantic/signature accounting across lexical resolution, phrase resolution, Phase-3 composition, candidate scoring, and round-trip validation;
- fix the root cause rather than special-casing `credit card`;
- add regression coverage for multi-word lexical concepts and duplicate/self-relation prevention.

### 3. Quality/status consistency defects

Human-test reproduction A:

- input: `🚲`
- output: `Bicycle.`
- visible semantic score: 100/100
- v0.6.0 UI simultaneously showed a red failure marker.

Human-test reproduction B:

- input: `robot zxqvplmno credit card bicycle fox`
- v0.6.0 output collapsed to only `🚲` while the visible quality metrics still showed 100/100.
- several known concepts (`robot`, `credit card`, `fox`) were silently lost and the intentional unknown token was not visibly represented in the released result.

Required fix:

- make quality/status reporting consistent with the released winner and actual validation metrics;
- identify every hidden metric that can force a failure state despite a correct winner;
- add input-to-output coverage accounting that prevents a subset-only translation from receiving PASS/100-quality when required concepts were dropped;
- never silently discard known concepts or unresolved tokens without explicit coverage/unknown evidence;
- never show PASS-grade metrics for materially incomplete output;
- add regression coverage for winner/status consistency in Emoji -> ABC mode and multi-concept coverage integrity in ABC -> Emoji mode.

### 4. Qwen/Ollama fallback, learning, and observability

Human-test evidence:

- input: `hopping`
- deterministic result: unresolved/unknown;
- `🦙🔍` probe returned `❌🦙📡` in the installed v0.6.0 app;
- runtime source is configured for local Ollama at `http://127.0.0.1:11434` and model `qwen2.5vl:7b`;
- Qwen is intended as a semantic teacher/fallback after a genuine deterministic FAIL, but the current user experience collapses provider-unavailable and genuinely-unresolved states into essentially the same visible failure.

Required fix:

- verify and harden the complete escalation chain: Forgey primary -> deterministic authority -> learned mapping -> Qwen/Ollama assist -> deterministic validation -> learning persistence;
- expose enough user-facing diagnostics to distinguish provider unavailable, provider error, assisted candidate rejected, learned candidate rejected, and genuinely unresolved input;
- never expose raw provider text directly as trusted EL output;
- only persist/reuse learned mappings that pass the deterministic validation/revalidation contract;
- add regression tests proving Qwen is called only at the intended FAIL boundary and that validated learning can be reused later without another provider call.

### 5. Top-right Settings button (`⚙️`)

Add a persistent user-facing `⚙️` Settings button at the top-right of the EL Bot window.

The Settings surface must be the canonical place for user-available configuration. It should open a clear settings panel/modal without disrupting the current translation session.

#### Ollama / local AI settings

At minimum, provide:

- current Ollama connection status;
- base URL / endpoint, defaulting to `http://127.0.0.1:11434`;
- selected model, defaulting to the currently supported `qwen2.5vl:7b` unless the runtime contract changes;
- `Test Connection` / probe control;
- explicit states for Ollama unreachable, Ollama reachable but model missing, and ready;
- safe model discovery/selection from locally available compatible models where supported;
- validation before saving invalid endpoints/models;
- clear indication of whether Qwen fallback is currently available to translation.

#### Forgey settings/status

Expose only safe user-facing Forgey controls and evidence, including as appropriate:

- active Forgey generation/model identity;
- Forgey readiness/status;
- local model/runtime health and relevant diagnostics;
- user-adjustable Forgey options only when they are supported by the validation/safety contract;
- no hidden developer-only or unsafe bypass controls in the normal Settings UI.

#### Updater settings

The same Settings surface should include updater preferences such as:

- automatic update checks on/off;
- manual `Check for Updates`;
- current installed version;
- last successful update check/result;
- update channel only if more than one trusted channel is intentionally introduced later.

#### General settings contract

- settings must persist across app restarts and upgrades;
- changes must be validated before persistence;
- provide `Reset to Defaults` for user-available settings;
- preserve current translation/session state when opening/closing Settings;
- settings failures must be visible and non-destructive;
- no setting may silently weaken release verification, deterministic validation, or provider-safety boundaries.

### 6. Translation request lifecycle / repeated Enter defect

Human-test reproduction:

- while a translation is processing, repeatedly pressing Enter starts/restarts processing again;
- after a translation has already completed, repeated Enter can immediately launch the same translation again;
- this was observed during the HARD test batch with long-running Forgey G2 inference.

Required fix:

- introduce a single authoritative translation request lifecycle with an explicit in-flight request identity;
- prevent duplicate submission of the same request while it is already running unless the UI explicitly supports cancel/restart;
- disable or debounce Run/Enter while the active request is in flight, or make repeated activation idempotent;
- prevent stale/older async results from overwriting a newer request;
- after completion, require a deliberate new submission rather than accidental key-repeat retriggering;
- define explicit behavior for cancellation, superseding input, and rapid direction changes;
- add automated tests for Enter key repeat, repeated Run clicks, request supersession, stale-result rejection, and post-completion key repeat.

### 7. Long-input semantic explosion / compression-quality defect

Human-test reproduction:

- a long paragraph about repeatedly waiting for a train completed without crashing, but produced a very large, repetitive EL sequence with repeated hourglass/train/relation motifs;
- an extreme repeated long-input stress test also completed, but produced an enormous dense emoji wall, visible unresolved count (~37), and quality around the low-80s despite highly repetitive source text;
- UI remained responsive and processing stage progression was visible, so this is not a crash/performance failure; it is an output-quality/representation-efficiency problem.

Required fix:

- investigate whether repeated semantic units are being independently emitted instead of canonicalized/compressed;
- distinguish intentional repeated meaning from accidental duplicate candidate/signature expansion;
- preserve important repetition when it changes meaning, but avoid mechanically re-emitting identical relation chains for repeated clauses;
- introduce measurable output-size/semantic-density diagnostics for long inputs;
- add regression tests for long repetitive prose, long non-repetitive prose, maximum supported input size, and output-growth bounds.

### 8. Image-path provider dependency / native-vision routing defect

Human-test evidence:

- an image/screenshot was successfully loaded and previewed in v0.6.0;
- the resulting visible status was effectively `❌ 📸 -> 🦙 OFF`, showing terminal image failure associated with Ollama/provider unavailability;
- the certified package contains native Forgey vision and provider-free vision evidence, so the runtime image path must be audited to determine why an unavailable Ollama provider becomes the user-visible terminal state.

Required fix:

- audit image routing from UI input -> Forgey/native vision -> deterministic validation -> optional provider assist;
- ensure an unavailable Ollama/Qwen provider does not incorrectly bypass or mask a valid native Forgey vision path;
- if native vision cannot support the supplied image, report that separately from provider-offline status;
- expose which vision stage handled/rejected the image;
- add offline image tests proving provider-free native vision still executes where supported;
- add provider-assisted image tests separately, with truthful fallback/availability state.

## Current human-test ledger feeding v0.7.0

### Easy tests

- install / first launch: PASS
- `fox` -> `🦊`: PASS
- `laptop` -> `💻`: PASS
- `chair` -> `🪑`: PASS
- `robot` -> `🤖`: PASS
- `credit card`: FAIL, duplicate composition defect
- clear/reset: PASS
- `🚲` -> `Bicycle.`: translation PASS, status/UI PARTIAL/FAIL
- `fox` via Run button: PASS

### Medium tests

- `words`: PASS
- `converter`: PASS
- `ChatGPT`: PARTIAL; sensible semantic composition but imperfect round-trip score (~100/67 observed)
- empty input: PASS controlled hold/no crash
- `zxqvplmno`: PASS intentional unresolved control
- `credit card` alternatives: confirms duplicate composition/ranking defect
- `hopping`: unresolved; Ollama/Qwen probe subsequently confirmed unreachable (`❌🦙📡`)

### Hard tests

- critical server/restart sentence: semantic retention largely PASS; critical/error/time/restart/service/success concepts remained represented, but quality/status was not clean, so keep as a regression case.
- long repeated train paragraph: functional PASS (completed, UI stayed usable), output-quality PARTIAL because of repetitive EL expansion and unresolved content.
- mixed known + unknown (`robot zxqvplmno credit card bicycle fox`): FAIL; output collapsed to `🚲` while visible metrics showed 100/100, proving semantic coverage/quality integrity defect.
- repeated Enter during/after processing: FAIL; duplicate/restarted processing requests are accepted instead of being lifecycle-controlled/idempotent.
- image/screenshot input while Ollama unavailable: image preview PASS, translation FAIL/diagnostic; visible result tied failure to `🦙 OFF`, requiring native-vision/provider routing audit.
- extreme wide resize: PASS; side-by-side layout remained usable.
- tall/narrow resize: PASS; layout reflowed vertically and controls remained usable.
- long-running processing visuals: PASS for visible stage progression; observed ABC -> Emoji engine followed by `Forgey Insta — G2 primary inference`, with hourglass/timer remaining active.
- extreme repeated long-input stress: functional PASS/no crash, output-quality PARTIAL due dense repetitive output and many unresolved units.
- built-in `🧪` diagnostics: not yet human-tested in this batch.

## Acceptance gate for v0.7.0

v0.7.0 is not releasable until:

1. all locked v0.6.0 human-test defects have regression tests and verified root-cause fixes;
2. the updater passes install/update/restart/version-verification and network-failure recovery tests;
3. the `⚙️` Settings surface works and persists supported user settings;
4. Ollama/Qwen availability and fallback states are truthful and observable;
5. Forgey/deterministic/provider validation boundaries remain intact;
6. request lifecycle is idempotent/safe under repeated Enter/Run input and stale async results cannot overwrite newer work;
7. semantic coverage metrics cannot report PASS/100 for materially incomplete multi-concept output;
8. native vision and provider-assisted vision are independently testable and truthfully reported;
9. long-input output growth and semantic density meet defined bounds without losing required meaning;
10. existing Phase-6 package/runtime/vision/44-engine regressions remain green;
11. human EASY, MEDIUM, HARD, IMAGE, OFFLINE, reboot, uninstall/reinstall, and update-path tests pass as applicable;
12. release publication again passes the transactional remote release verification contract.

## Change-control rule

Continue collecting v0.6.0 human-test evidence before implementing this batch. Do not hide or silently reinterpret failures. Investigate each defect before fixing it, then implement v0.7.0 as a coherent upgrade with updater + settings + validated defect fixes.
